import React from "react";
import {Text, View, StyleSheet} from "react-native";

const PrestamosPantalla = () => {
    return(
        <View>
            <Text style={{fontSize:30, alignItems: 'center', marginTop:20}}>Pantalla prestamos</Text>
        </View>
    );
}

export default PrestamosPantalla;